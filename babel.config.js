const minified = true;
const comments = false;

const presets = ["@babel/preset-typescript"];

const plugins = ["babel-plugin-add-import-extension"];

export default { presets, plugins, minified, comments };
